# Pinned to the versions verified in CI/dev. The Docker image installs
# exactly these so builds are reproducible.
cloakbrowser==0.5.3
beautifulsoup4==4.15.0
lxml==6.1.1
playwright==1.62.0
apify==4.0.0
PyYAML==6.0.3
